pre-commit:
  commands:
    cargo-fmt:
      glob: "src-tauri/*.rs"
      run: cargo fmt --manifest-path src-tauri/Cargo.toml
      stage_fixed: true

pre-push:
  commands:
    frontend-checks:
      run: bun run check:frontend
